1) download any random image from google.
2) copy that image into paint and convert the jpeg to bitmap image(BMP).
3) can be converted through paint while saving the image.
4) xiao stegnography tool requires only BMP format image.
5) open the tool and load the bmp image .
6)create the text file u want to send the secret message to .
7) upload the secret message text file.
8)choose the encryption details.
9)rc2 and md5 as the encryption details and then write confidential password which is in here is (Saksham).
10) save the secret message and send to the requested user to decrypt it.
11) to decrypt use the same tool and select extract to opt and it will extract the secret message when entered the correct password.